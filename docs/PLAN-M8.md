# Plan — M8: `บิล` (Phase 2 ก้อนแรก)

ขอบเขตรอบนี้: **พิมพ์ `@บิลใหญ่ บิล` แล้วได้รายการบิลที่กดดูรายละเอียดรายใบได้** (D45)
· และ **บังคับ mention ให้คำสั่งคีย์เวิร์ดทุกตัวเมื่ออยู่ในกลุ่ม** (D47)

รอบนี้เป็นฝั่งอ่านล้วน ไม่มีอะไรถูกเขียนลง DB เลยสักแถว

**ที่มา:** การ์ด `ยอด` ไม่ยุบข้ามคนตาม D5 จริง แต่ยุบข้ามบิลของคู่เดียวกัน — เดียร์ติดนัท
900 คือสามใบรวมร่างเป็นเลขเดียวที่ไล่ไม่ได้ว่ามาจากไหน · ทางออกคือคำสั่งใหม่ ไม่ใช่แก้
การ์ดเดิม เพราะสองอันตอบคนละคำถาม (`ยอด` = ตอนนี้ใครติดใคร · `บิล` = เกิดอะไรขึ้นบ้าง)

---

## กติกาที่บังคับ (ต่อจาก M1–M7)

- **TDD จริง** — เขียนเทสต์ให้แดงก่อน ห้ามสลับ
- **mutation test ทุกครั้งที่เทสต์เขียวตั้งแต่รอบแรก** — และตรวจเสมอว่าการกลายพันธุ์ถูกใส่จริง
- ห้าม `any` ห้าม `as` ที่ไม่จำเป็น ห้าม `console.log` ค้าง ห้าม `.skip`
- `lib/contract.test.ts` + `lib/db/contract.db.test.ts` **ห้ามลบห้ามผ่อนเกณฑ์**
- **D25 — Debt คิดใน `lib/debt.ts` ที่เดียว** · การ์ด `บิล` ไม่แตะสูตรหนี้เลยแม้แต่นิดเดียว
- **repo เป็น public** — ห้ามมี secret, เบอร์จริง, `groupId`/`userId` จริง

---

## สิ่งที่ทำ

### 1. D47 — คำสั่งคีย์เวิร์ดในกลุ่มต้อง mention

**เปลี่ยนพฤติกรรมของ `ยอด` ที่ขึ้น production ไปแล้ว** ทำก่อนข้ออื่นเพราะข้ออื่นสร้างอยู่บนมัน

| ที่ไหน | พิมพ์ | ผล |
|---|---|---|
| กลุ่ม | `@บิลใหญ่ ยอด` · `@บิลใหญ่ บิล` | คำสั่ง |
| กลุ่ม | `ยอด` · `บิล` เปล่าๆ | **เงียบ** |
| กลุ่ม | `+ ข้าว 300` | จดบิล — **ไม่แตะ** |
| 1:1 | `ยอด` · `บิล` เปล่าๆ | คำสั่ง — ไม่บังคับ mention |

เกณฑ์ 1:1 ยืมมาจากกฎเงียบตรงๆ (`lib/flow/dispatch.ts`): ที่นั่นไม่มีบทสนทนาของใครให้แทรก
เหตุผลของกฎหายไป กฎจึงไม่ตามไปด้วย · **และ LINE ไม่มี mention ในแชท 1:1 อยู่แล้ว**

**D34 หดลง** — เงื่อนไข "ส่วนต่อท้ายเป็น `#tag` ล้วนถึงจะนับเป็นคำสั่ง" มีไว้กัน `ยอด มาไหม`
ซึ่งตอนนี้ถูก mention กันให้หมดแล้ว · **ยังไม่ลบทิ้งในรอบนี้** — `@บิลใหญ่ ยอด #เชียงใหม่`
ยังต้องตอบว่ายังไม่เปิดใช้ ไม่ใช่ตอบยอดทั้งวงเงียบๆ

### 2. parser รู้จัก `บิล`

- `lib/types.ts` — `BotCommand` เพิ่ม `'bills'`
- `lib/parser/rules.ts` — คีย์เวิร์ด `บิล`
- `lib/line/messages.ts` — `COMMAND_HELP` เพิ่มบรรทัด `บิล — รายการบิลที่จดไว้`

### 3. `lib/repo/views.ts` — สองตัวอ่าน

```
loadBillList(group, limit)   →  N ใบล่าสุด + จำนวนทั้งหมด
loadBillDetail(expenseId)    →  บิลใบเดียวพร้อมรายคน
```

- `listExpenses` มีอยู่แล้ว รับ `limit` และกรอง `status='active'` ให้เรียบร้อย
- เรียงใหม่→เก่า: `spent_at desc, created_at desc, id desc` (มีอยู่แล้วในตัวมัน)
- **`listMembers` ต้องส่ง `{ includeLeft: true }`** ไม่งั้นชื่อคนที่ออกจากกลุ่มไปแล้วหายจาก
  บิลเก่า ซึ่งใน ledger คือยอดที่อ่านไม่รู้เรื่อง
- `loadBillDetail` **ต้องตรวจว่าบิลอยู่วงเดียวกับคนกด** — postback มาจากการ์ดที่ลอยอยู่ใน
  แชทได้ตลอดกาล ห้ามเชื่อว่า id ที่ส่งมาเป็นของวงนี้

### 4. `lib/flow/bills.ts` — จัดรายการเป็นการ์ด (TDD, บริสุทธิ์)

รับผลจาก repo คืนโครงการ์ด · ไม่มี I/O ไม่มีสูตรหนี้

- แถวหนึ่ง = ชื่อบิล + วันที่ + ยอดรวม · **ไม่มีรายชื่อคน** เพื่อให้แถวหนักคงที่ราว 250 ไบต์
  ไม่โตตามขนาดวง (นี่คือทั้งหมดของ D45)
- ท้ายการ์ดเขียนจำนวนที่เหลือเมื่อตัด — **ห้ามตัดเงียบ** (D31/D44)
- การ์ดรายละเอียด: ชื่อ + วันที่ + ยอดรวม + รายคน + ป้ายคนจ่าย — โครงเดียวกับการ์ด Draft
  **แต่ไม่มีปุ่ม** เพราะบิลลงไปแล้ว

### 5. postback

```
bill=<expenseId>
```

- **`expense.id` (uuid 36 ตัว) เท่านั้น ห้ามพาชื่อ** — ชื่อไทยผ่าน `encodeURIComponent`
  ยาวขึ้นเก้าเท่า ทะลุเพดาน 300 ตั้งแต่ชื่อยาวราว 27 ตัว (ADR 0002)
- รูปแบบเดียวกับ `confirm=<draftId>&as=<memberId>` ที่ใช้อยู่ — คนละ key จึงแยกกันได้

### 6. เคสที่ไม่ใช่ "มีบิล"

| สถานะ | ตอบอะไร | ทำไม |
|---|---|---|
| วงยังไม่มีบิลสักใบ | **ไกด์** | เกณฑ์เดียวกับ `ยอด` ในวงที่ยังไม่เคยจด |
| กดแถวของบิลที่ถูกยกเลิกแล้ว | **บอกว่าถูกยกเลิกแล้ว** | การ์ดเก่าลอยอยู่ในแชทและกดได้เสมอ (D30 ไม่มี hard delete) · เงียบอ่านออกว่าบอทพัง · โชว์ยอดเก่าคือตัวเลขผิดใน ledger |
| กดแถวของบิลที่อยู่คนละวง | **บอกว่าหาไม่เจอ** | ไม่บอกว่ามีอยู่จริงในวงอื่น |
| การ์ดใหญ่เกิน bubble | **ลดรูปเป็น text** | ทางเดิมของ D44 · เกิดยากกว่า `ยอด` มากเพราะแถวไม่โตตามขนาดวง |

### 7. ต่อสาย

- `lib/flow/dispatch.ts` เพิ่ม `'bills'` เข้า `IMPLEMENTED_COMMANDS` — **ที่เดียว**
  แล้วไกด์จะพูดถึง `บิล` เอง (`messages.test.ts` บังคับให้ตรงกันอยู่แล้ว)
- webhook เพิ่ม dependency อ่านรายการกับรายละเอียด · **DB ล่ม → 500** ตามเกณฑ์เดิม
- `บิล` ในแชท 1:1 อ่านวงส่วนตัวของคนนั้น เหมือนเส้นทางเดิมทุกประการ

---

## ค่าคงที่

```
BILL_LIST_LIMIT = 20
```

20 แถว ≈ 5 KB เหลือที่ให้หัวและท้ายการ์ด · เพดาน bubble 10 KB · แถวหนัก ~250 ไบต์คงที่

---

## เกณฑ์ว่าเสร็จ

```
npm test            510 + ของใหม่ เขียว
npm run test:db     364 + ของใหม่ เขียว
npx tsc --noEmit    ผ่าน
npm run build       ผ่าน
grep -rn "any|console.log|.skip" lib app     ไม่เจอของค้าง
```

แล้ว `/code-review high` · ตรวจ finding เองก่อนรายงาน

**ยิงของจริง** — ในกลุ่ม LINE จริง:

| ทำอะไร | ต้องได้ |
|---|---|
| `ยอด` เปล่าๆ ในกลุ่ม | **เงียบสนิท** (เปลี่ยนจากเดิม — D47) |
| `@บิลใหญ่ ยอด` | การ์ดยอดเหมือนเดิมทุกประการ |
| `บิล` เปล่าๆ ในกลุ่ม | **เงียบสนิท** |
| `@บิลใหญ่ บิล` ในวงที่ยังไม่เคยจดบิล | ไกด์ |
| `@บิลใหญ่ บิล` หลังจดสามใบ | สามแถว ใหม่→เก่า มี `›` บอกว่ากดได้ |
| กดแถวหนึ่ง | การ์ดรายละเอียดใบนั้น ยอดรายคนตรงกับตอนจด · ป้ายคนจ่ายถูกคน |
| กดแถวเดิมซ้ำ | การ์ดเดิม ไม่มีอะไรเปลี่ยนใน DB |
| `ยอด` ใน 1:1 | ยังใช้ได้ ไม่ต้อง mention |
| `บิล` ใน 1:1 | รายการของวงส่วนตัว |
| `+ ข้าว 300 กอล์ฟ` | จดได้เหมือนเดิม **ไม่ต้อง mention** |

**ตรวจใน DB หลังทดสอบ:** ทุก query ในรอบนี้อ่านอย่างเดียว — `select count(*) from expense`
กับ `expense_share` ต้องเท่าเดิมเป๊ะก่อนและหลังกดดูรายละเอียดกี่ครั้งก็ตาม

**วัดโควตา** — `totalUsage` ต้องไม่ขยับ

---

## ที่ไม่อยู่ใน M8

- **ปุ่ม `ดูทั้งหมด` → LIFF (D46)** — รอ Phase 2 มี LIFF ก่อน · ระหว่างนี้ท้ายการ์ดเขียน
  จำนวนที่เหลือแทน ซึ่งไม่ขัด D31 เพราะไม่ได้เงียบๆ ตัด
- `บิล <ชื่อ>` / `บิล #tag` / `บิล 50` — ขยายทีหลังโดยไม่ต้องรื้อ
- ลบเงื่อนไข `#tag` ล้วนของ D34 — ยังไม่แตะจนกว่า tag จะเปิดใช้จริง

---

## ยิงของจริงแล้ว — 4 ก.ย. 2026

ยิงบน **preview ของ `dev` ผ่าน dev OA (`BillYai(Dev)`)** ไม่ใช่บน production — ต่างจาก
M4–M7 ที่ยิงบน production ทุกรอบเพราะตอนนั้น Preview ยังไม่มี `LINE_CHANNEL_SECRET`
ที่ถูกให้ตั้ง · เหตุผลที่เปลี่ยนทาง: **D47 เปลี่ยนพฤติกรรมของที่ขึ้น production ไปแล้ว**
`ยอด` เปล่าๆ ในกลุ่มเคยตอบ merge แล้วจะเงียบ — ทดสอบบน production คือให้ผู้ใช้เจอ
ของที่ยังไม่ได้ยืนยันก่อนพิสูจน์ · วิธีตั้ง dev OA อยู่ใน `SETUP-DEPLOY.md` §2.5(4)

ไล่ตารางข้างบนครบทั้งชุด **ผลตรงทุกข้อ** — รวมข้อที่รีวิวจับได้ว่าเคยหาย: กดแถวของบิล
ที่ระบุชื่อคนแล้ว การ์ดยังบอก `จ่ายโดย <คนพิมพ์>` ทั้งที่เขาไม่มีแถวใน `expense_share`
ตาม D43

### ที่วัดได้

**`บิล` กับการกดแถวอ่านล้วนจริง** — `count(*)` ของ `expense` กับ `expense_share` เป็น
`9 / 17` ทั้งก่อนและหลังกดแถวซ้ำสามครั้ง · นี่คือหลักฐานชิ้นเดียวที่พิสูจน์ข้ออ้างของ
D45 ได้ เพราะเทสต์เห็นแค่โค้ดของตัวเอง ไม่เห็นว่า postback เดินทางจริงแล้วเกิดอะไรใน DB

**invariant ของสคีมาผ่านทุกบิล** — query ที่เทียบ `Σ amount_satang` กับ
`round(total_satang * (1 + surcharge_pct/100))` ของทุกใบที่ `status='active'` คืน **0 แถว**

**`totalUsage` = 0** — ยิงครบทั้งชุดแล้วโควตายังไม่ขยับ · ด่านเดียวที่จับได้ว่ามีเส้นทาง
ไหนเผลอไปเรียก push แทน reply (C2)

### ที่โผล่มาระหว่างยิงจริง — ใบเสร็จร้านอาหารที่แต่ละคนกินไม่เหมือนกัน

เคสจริงที่เจอ: บิงซู 7 รายการ ยอดรวม 487 บาท คนกินแต่ละรายการไม่ใช่ชุดเดียวกัน ·
ไวยากรณ์ในแชทรับบิลแบบนี้ตรงๆ ไม่ได้ **โดยตั้งใจ** — นั่นคือ `split_mode='itemized'`
ซึ่ง D6 วางไว้เป็นงานของ LIFF (`DESIGN.md` — "ใช้เฉพาะฝั่ง LIFF ที่หารรายชิ้น")

**แต่ทำได้วันนี้โดยไม่ต้องรอ LIFF: บิลหนึ่งใบไม่ต้องเท่ากับใบเสร็จหนึ่งใบ** — ยุบรายการ
ที่มี**ชุดคนกินเหมือนกัน**เข้าเป็นก้อนเดียว แล้ว**ตัดก้อนที่มีแต่คนจ่ายทิ้ง** เพราะไม่มี
ใครติดใคร

| ชุดคนกิน | รวม | พิมพ์ |
|---|---|---|
| four nut dear aek | 278 | `+ บิงซูรวม 278 four nut dear aek` |
| game | 179 | `+ ฮันนี่โทสต์นมสด 179 game` |
| nut (คนจ่าย) | 30 | — ไม่ต้องจด |

7 รายการเหลือ 2 ข้อความ และหนี้ออกมาเท่ากันเป๊ะ: `69.50 × 3 + 179 = 387.50` ที่คนอื่น
ติด nut บวกส่วนของ nut เอง `69.50 + 30 = 99.50` รวม **487** ตรงยอดจริง

**ต้องใส่ชื่อคนจ่ายในก้อนที่เขาร่วมกินด้วย** — D43 ตัดคนจ่ายออกจากการหารเมื่อระบุชื่อคน
ลืมใส่แล้วจะกลายเป็นหาร 3 แทน 4

ราคาที่จ่าย: การ์ด `บิล` เห็นชื่อก้อนที่ยุบแล้ว ไม่ใช่ 7 รายการจริง — ไล่กลับไปดูว่าใคร
กินอะไรไม่ได้ ซึ่งคือสิ่งที่ itemized มาแก้

### ที่ไม่ได้ทดสอบรอบนี้

- **กดแถวของบิลที่ถูกยกเลิก** — ยังไม่มีคำสั่งยกเลิกบิล (`IMPLEMENTED_COMMANDS` มีแค่
  `guide` `balance` `bills`) จึงสร้างสถานะ `voided` จากในแชทไม่ได้ · โค้ดมีทางลงแล้ว
  และ db test คุมอยู่ แต่ยังไม่เคยผ่านมือคนกด
- **การ์ดใหญ่เกิน bubble** — ต้องมีบิลหลายสิบใบถึงจะถึงเพดาน
